---
name: chinese-code-comment-skill
description: 综合编排中文代码注释处理：依次翻译现有注释、按 why 规范补充完整中文注释，再统一为目标语言的编程注释规范；当用户要求一次完成这三类工作时使用。
metadata:
  short-description: 依次翻译、补充并规范化中文代码注释
---

# 中文代码注释综合处理

这是综合 skill，负责按固定顺序调用三个独立 skill，避免把翻译、内容补全和格式规范混成一次不可核对的修改：

1. `chinese-code-comment-translate`：只把现有注释或文档字符串中的英文及其他语言翻译成准确中文。
2. `chinese-code-comment-enrich`：按照 why 优先和完整覆盖规范，补充类型、成员、函数参数、复杂语法及每条控制流路径的中文解释。
3. `chinese-code-comment-style`：让注释符合目标语言、项目配置和文档工具的编程规范。

综合 skill 的显式调用命令是 `chinese-code-comment-skill`。三个子 skill 也可以单独调用；用户只需要其中一项时，不要自动执行完整流水线。

## 范围和边界

- 默认只修改注释、文档字符串、JSDoc/Javadoc/Doxygen/XML 文档和与注释格式直接相关的文档标记，不改变业务逻辑、公共 API、变量类型、字符串字面量、日志、错误文本或数据格式。
- 跳过生成文件、依赖目录、压缩文件和二进制文件，除非用户明确要求处理。
- 注释必须忠实于代码、测试、调用方和项目文档；无法确认的语义标记为“需要业务确认”，不能写成猜测。
- 任一阶段发现需要改代码才能完成时，保留问题说明并停止该项代码修改，等待用户明确授权。

## 工作流程

1. **先检查 Context7 MCP**：查看当前 Codex 会话实际暴露的 MCP 工具或服务器。若有 Context7 且目标涉及第三方库、框架、语言特性或版本相关语义，优先查询与项目版本匹配的权威文档；Context7 不替代仓库源码、锁文件和测试。
2. **确认范围并保存基线**：识别目标语言、版本、项目注释风格、格式化器和文档工具；记录目标文件与原始差异，确保后续每一阶段只产生授权范围内的注释变化。
3. **调用翻译 skill**：使用 `chinese-code-comment-translate`，只翻译已有注释中的非中文文本，保留 API 名称、代码片段、标签、链接和技术标识；不要在这一阶段新增缺失注释。
4. **调用增强 skill**：使用 `chinese-code-comment-enrich`，对翻译后的结果补齐 why 优先的声明、成员、参数、复杂语法和每个控制流分支及可达路径注释；逐项核对每个类属性、结构体变量和成员声明。
5. **调用规范 skill**：使用 `chinese-code-comment-style`，统一注释语法、文档标签、位置、缩进、换行、术语和项目 lint/formatter 要求；不削弱前两阶段已经写明的原因、边界和影响。
6. **阶段性复核**：每个子 skill 完成后检查 `git diff`，确认没有改变代码行为、接口、字符串或错误处理；若子 skill 不可发现，尝试从本仓库对应目录加载其 `SKILL.md`，仍缺失时报告未完成，不得假装已执行。
7. **最终验证和交付**：运行可用的格式化、静态检查、编译或测试；至少运行 `git diff --check`，并按翻译、覆盖、路径和格式四张清单复核。交付时列出三个阶段实际修改的文件、未确认的假设和验证结果。

## 调用示例

完整流水线：

```text
请使用 chinese-code-comment-skill，处理 src/ 目录中的 Python 和 C++ 文件：先把现有英文注释翻译成中文，再按 why 规范补齐每个类型、成员、函数参数、复杂语法和每个 if/else/for/while 路径的注释，最后按项目的注释规范整理格式。只改注释，不改变代码逻辑。
```

只执行单项工作时，直接调用对应子 skill：

```text
请使用 chinese-code-comment-translate，只把 src/legacy.py 中已有英文注释和 docstring 翻译成中文；不要新增注释，也不要改字符串字面量。
```

```text
请使用 chinese-code-comment-enrich，为 src/queue.cpp 按 why 规范补充中文注释，逐一覆盖 class 成员、struct 字段、构造器参数以及每个分支、循环退出和异常路径。
```

```text
请使用 chinese-code-comment-style，检查 src/api.ts 的 JSDoc 是否符合项目 TypeScript 规范，修正标签、位置、换行和参数名，但保留注释含义，不改业务代码。
```

## 完成前自检

- 三个阶段确实按翻译 → 增强 → 规范的顺序完成，没有并行或跳过阶段。
- 翻译阶段没有把代码标识符、字符串字面量或 API 名称误译，也没有凭空新增语义。
- 增强阶段覆盖每个类型、每个成员/结构体变量、每个函数参数、每个复杂语法和每个控制流分支及可达路径。
- 规范阶段使用目标语言合法的注释语法，文档标签与真实签名一致，注释紧邻被解释的声明或代码块。
- 最终差异只涉及注释及用户明确授权的文档范围，验证命令和未确认事项已记录。
